# 第四阶段封版验收记录

日期：2026-04-07  
执行人：Codex（本地手工验证）  
环境：Windows / PowerShell / Node 24.13.0 / npm 11.6.2

---

## 一、封版结论

- 第四阶段已于 2026-04-07 通过封版验收。
- 当前仓库已满足第四阶段“基础重构期”的交付目标，可以开始转入第五阶段“业务闭环期”准备与实施。
- 第四阶段未继续展开的业务能力，不再视为本阶段缺陷，而是按计划转入第五阶段承接。

---

## 二、验收依据

- [第四阶段计划.md](/d:/1rjkf/syxt/plan/第四阶段计划.md)
- [第四阶段验收问题清单.md](/d:/1rjkf/syxt/plan/第四阶段验收问题清单.md)
- 当前 `server/` 目录下的路由、数据库访问层、迁移脚本与验收脚本实现

---

## 三、本次执行的验收项

### 1. 第四阶段 smoke test

执行命令：

- 在 `server/` 目录执行 `npm run accept:phase4`

验收结果：

- 通过
- 已覆盖关键字段类型校验
- 已覆盖 `device_id` 入库、`transactions.items` 占位写入、库存写入 `inventory`、`products.stock` 不再被订单链路改写等关键行为

### 2. 字段类型与迁移记录复验

执行命令：

- 在 `server/` 目录执行 `node scripts/check_schema_details.js`

验收结果：

- `transactions.business_date = bigint(20)`
- `order_items.price_snapshot = decimal(10,2)`
- `_migrations` 中已存在 `001 / 002 / 003 / 20260401_offline_base`

### 3. 空库迁移链路复验

复验内容：

- 使用临时空库执行 `node migrate.js`
- 确认基线迁移不再硬编码切换数据库
- 确认迁移链路可从空库完整落地到第四阶段目标结构

验收结果：

- 通过
- `001_initial_schema.sql` 现已尊重当前 `DB_NAME`
- 迁移链路可重复执行并正确写入迁移记录

### 4. 初始化链路复验

复验内容：

- `GET /auth/init-status`
- `POST /auth/init-setup`
- `POST /auth/login`

验收结果：

- 通过
- 初始化前 `initialized = false`
- 初始化后 `initialized = true`
- 可完成一次从空库到首商户、首管理员、首店、首设备的完整初始化

### 5. 库存拆层回归复验

复验内容：

- 新建测试商品并初始化库存
- 下单扣减库存
- 再次同步同商品但传入新的 `stock`
- 以总部管理员身份不带 `store_id` 查询商品

验收结果：

- 通过
- `inventory` 扣减正确
- 重新同步商品资料不会覆盖现有库存
- 跨门店查看商品时库存按门店聚合，不再返回重复商品行

---

## 四、与第四阶段目标的对齐结论

### 1. 已完成并通过验收的目标

- 初始化流程已从“公开注册开店”重构为正式初始化流程
- `products` 与 `inventory` 已完成职责拆分
- 内部主键已完成 UUID 统一，业务单号采用确认过的过渡方案
- 新订单已不再依赖 `transactions.items` JSON 作为主明细来源
- 离线底座关键字段已完成落地，设备绑定已进入订单主链路
- 版本化迁移、基础回滚入口与第四阶段 smoke test 已建立

### 2. 按计划延后到第五阶段的内容

- `refund_items` 驱动的按商品部分退款完整闭环
- `order_payments` 驱动的在线混合支付正式闭环
- `store_prices` 驱动的门店差异价格正式业务闭环
- 对账中心正式落地
- 审计日志查询页面完善

这些内容属于第五阶段范围，不再作为第四阶段封版阻塞项。

---

## 五、封版后续动作

1. 第四阶段状态正式标记为“验收通过”
2. 第五阶段以 [第五阶段计划.md](/d:/1rjkf/syxt/plan/第五阶段计划.md) 作为开工基线
3. 后续所有 schema 变更继续沿用版本化迁移，不再回退到直接手改总 schema 的方式
