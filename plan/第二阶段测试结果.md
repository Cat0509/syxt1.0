# 第二阶段测试结果（2026-03-18）

## 测试环境

- 运行方式：本机 PowerShell
- 数据库：MySQL（`ruyi_pos`）
- 服务端：`syxt/server`（Node.js + Express）

## 数据库连通性检查

- 脚本：`server/test_connection.js`
- 结果：连接成功，表结构存在（`audit_logs`、`inventory_movements`、`merchants`、`order_items`、`products`、`refunds`、`skus`、`stores`、`transactions`、`users`）
- 种子用户：`admin` / `manager1` / `cashier1` / `cashier2`

## 接口冒烟测试

测试流程：

1. `GET /health`
2. `POST /auth/login`（admin）
3. `POST /products/sync`（写入测试商品）
4. `POST /orders`（创建订单）
5. 重复 `POST /orders`（幂等性验证）
6. `POST /refunds`（退款申请）
7. `PATCH /refunds/:id/approve`（退款审批）
8. `POST /inventory/adjust`（库存调整）
9. `GET /reports/summary`（报表聚合）

结果总结：

- `health`：通过
- `login`：通过
- `products/sync`：通过
- `orders`：通过
- 幂等重复下单：HTTP 409 正常
- `refunds`：通过
- `refunds approve`：通过
- `inventory/adjust`：通过
- `reports/summary`：通过

## 已修复问题

1. 幂等冲突 HTTP 状态码不正确已修复  
2. 商品同步门店归属不明确已修复  

## 备注

本次为冒烟联调测试，未覆盖并发、权限边界、跨门店一致性与完整业务流程回归。

## 回归测试（2026-03-23）

- 数据库连通性检查：通过
- 接口冒烟测试：全部通过
- 幂等重复下单：HTTP 409
