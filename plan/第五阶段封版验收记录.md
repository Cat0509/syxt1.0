# 第五阶段封版验收记录

日期：2026-04-08  
执行人：Codex（本地手工验证，二次复验）  
环境：Windows / PowerShell / Node 24.13.0 / npm 11.6.2

---

## 一、封版结论

- 第五阶段于 2026-04-08 再次验收后，仍未通过封版。
- 当前仓库已经具备第五阶段的大部分核心能力，且较上次验收已有明显收口。
- 但在“审计日志查询闭环”和“对账中心定位差异订单”两项上仍未达到计划要求，因此暂不建议标记为封版完成。

---

## 二、验收依据

- [第五阶段计划.md](/d:/1rjkf/syxt/plan/第五阶段计划.md)
- [第五阶段验收问题清单.md](/d:/1rjkf/syxt/plan/第五阶段验收问题清单.md)
- [后续整体开发计划.md](/d:/1rjkf/syxt/plan/后续整体开发计划.md)
- 当前 `server/` 目录下的路由、数据库访问层、迁移脚本、前端收银逻辑与第五阶段 smoke test

---

## 三、本次执行的验收项

### 1. 第五阶段 smoke test

执行命令：

- 在 `server/` 目录执行 `node scripts/phase5_smoke_test.js`

验收结果：

- 通过
- 本次复验时间戳：`2026-04-08T01:10:37.800Z`
- 已覆盖门店差异价格、混合支付底层入库、部分退款审批、库存回补、对账接口基础返回、审计筛选基础可调用等主链路

### 2. 迁移记录复验

执行命令：

- 在 `server/` 目录查询 `_migrations`

验收结果：

- 已确认存在：
  - `005_phase5_biz_loop.js`
  - `006_add_audit_user_dimensions.js`

结论：

- 第五阶段新增表与审计用户维度变更均已进入版本化迁移体系。

### 3. 上次阻塞项复验

复验内容：

- 部分退款后查询 `reports/summary`
- 整单退款后查询 `reports/reconciliation`
- 检查前端是否已补齐混合支付 / 对账中心 / 审计日志页面入口

验收结果：

- 通过
- 部分退款订单已重新进入报表口径
- 整单退款已能计入对账退款金额
- 前端已补齐混合支付式结算交互
- 前端已补齐对账中心、审计日志入口

### 4. 审计日志按账号筛选复验

复验内容：

- 以管理员身份执行门店改价
- 通过 `username` 过滤 `STORE_PRICE_CHANGED` 审计日志

验收结果：

- 未通过
- 本地复验结果：
  - `ADMIN_NAME=系统管理员`
  - `AUDIT_FILTER_MATCHES=0`
  - `AUDIT_TOTAL_RETURNED=0`

结论：

- 审计查询参数虽然已补，但关键业务日志并未统一写入用户维度，无法满足“按账号追溯关键操作”的验收标准。

### 5. 对账中心差异定位能力复验

复验内容：

- 构造一笔部分退款和一笔整单退款均正常完成的场景
- 查询对账中心汇总结果

验收结果：

- 未通过
- 本地复验结果：
  - `RECON_RECEIVABLE=280`
  - `RECON_ACTUAL=280`
  - `RECON_REFUNDED=200`
  - `RECON_DISCREPANCY=-200`

结论：

- 当前对账中心只有汇总表，没有异常订单列表与跳转明细。
- `discrepancy` 口径在正常支付/退款场景下仍产生误报，不足以支撑“定位差异订单”。

---

## 四、与第五阶段目标的对齐结论

### 1. 已完成并通过验收的目标

- `refund_items` 已落地，支持按商品维度发起部分退款
- `order_payments` 已落地，后端支持一笔订单写入多笔支付明细
- `store_prices` 已落地，查询优先级已接入商品与 SKU 读取
- `transactions.status` 已正式支持 `partially_refunded`
- 报表与历史页已基本补齐“部分退款”状态口径
- 前端已出现混合支付结算交互
- 前端已出现对账中心与审计日志入口
- 第五阶段 schema 已通过版本化迁移落地
- 第五阶段 smoke test 已建立并可重复执行

### 2. 仍未通过验收的目标

- 审计日志尚未真正做到按门店、账号、时间、动作完整查询
- 对账中心尚未真正做到“可定位差异订单”

这两项属于当前封版阻塞项，详见 [第五阶段验收问题清单.md](/d:/1rjkf/syxt/plan/第五阶段验收问题清单.md)。

---

## 五、与上次验收相比的变化

### 1. 已关闭的上次阻塞项

- 部分退款订单被报表排除
- 对账中心不兼容整单退款 / 历史退款
- 前端仍是单一支付方式
- 前端缺少对账中心 / 审计日志入口

### 2. 仍保留的阻塞方向

- 审计日志账号维度没有贯穿到关键业务事件
- 对账中心尚缺异常订单定位能力，且差异公式口径不稳定

---

## 六、后续动作建议

1. 统一关键业务日志写入，全部带上 `user_id / username`。
2. 补齐审计页面的时间筛选与门店筛选器填充。
3. 修正对账中心差异口径，并补异常订单列表与明细跳转。
4. 修复后重新执行第五阶段 smoke test 与本次两条手工复现场景，再决定是否封版。
